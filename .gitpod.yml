image:
  file: .gitpod.Dockerfile

tasks:
  # - init: |

  - command: |
      mkdir data

vscode:
  extensions:
    - redhat.vscode-yaml
    - yy0931.vscode-sqlite3-editor
    - gruntfuggly.todo-tree
    - jakebecker.elixir-ls
